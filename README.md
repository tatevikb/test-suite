# test-suite
Automation task

## Getting started with Test Suite project

## Description
This project includes UI and API tests

## Installation
Clone repository in your local environment by using `git clone` command.

## Defining the credentials
In the credentials.properties add the credentials

## Usage
To run tests simply execute the following command: `gradle test`